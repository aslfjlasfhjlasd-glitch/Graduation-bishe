# 后端启动问题修复说明

## 🐛 问题描述

运行 `start.bat` 后，后端窗口自动关闭，无法正常启动。

## 🔍 错误信息

```
java.lang.IllegalArgumentException: Invalid value type for attribute 'factoryBeanObjectType': java.lang.String
```

## 🎯 根本原因

项目的 [`pom.xml`](backend/pom.xml) 中同时引入了两个MyBatis依赖：

1. **MyBatis-Plus** (com.baomidou:mybatis-plus-boot-starter:3.5.5)
2. **MyBatis Spring Boot Starter** (org.mybatis.spring.boot:mybatis-spring-boot-starter:3.0.3)

这两个依赖在Spring Boot 3.2.0环境下存在冲突，导致Bean工厂初始化失败。

## ✅ 解决方案

### 修改1：移除MyBatis-Plus依赖

**文件**：[`backend/pom.xml`](backend/pom.xml:20-23)

```xml
<!-- 移除这个依赖 -->
<!-- <dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-boot-starter</artifactId>
    <version>3.5.5</version>
</dependency> -->
```

**理由**：
- 项目中没有使用MyBatis-Plus的特性（如BaseMapper、IService等）
- 所有Mapper都是标准的MyBatis接口
- 保留标准MyBatis即可满足需求

### 修改2：添加@MapperScan注解

**文件**：[`backend/src/main/java/com/university/volunteer/VolunteerApplication.java`](backend/src/main/java/com/university/volunteer/VolunteerApplication.java:8)

```java
@SpringBootApplication
@MapperScan("com.university.volunteer.mapper")  // 明确指定Mapper扫描路径
public class VolunteerApplication {
    // ...
}
```

**理由**：
- 明确告诉Spring Boot在哪里扫描Mapper接口
- 避免自动扫描可能导致的问题

## 🚀 验证结果

执行以下命令验证修复：

```bash
cd backend
mvn clean compile spring-boot:run
```

**成功输出**：
```
Tomcat started on port 8080 (http) with context path ''
Started VolunteerApplication in 2.253 seconds
```

## 📝 修改文件清单

1. ✅ [`backend/pom.xml`](backend/pom.xml) - 移除MyBatis-Plus依赖
2. ✅ [`backend/src/main/java/com/university/volunteer/VolunteerApplication.java`](backend/src/main/java/com/university/volunteer/VolunteerApplication.java) - 添加@MapperScan注解

## 🔄 后续使用

### 方式1：使用start.bat（推荐）

直接双击 `start.bat`，会自动启动后端和前端。

### 方式2：手动启动

**后端**：
```bash
cd backend
mvn spring-boot:run
```

**前端**：
```bash
cd frontend
npm run dev
```

## ⚠️ 注意事项

1. **不要重新添加MyBatis-Plus**：除非你计划使用其特性，否则保持当前配置
2. **依赖冲突检查**：如果将来添加新依赖，注意检查是否与现有依赖冲突
3. **版本兼容性**：Spring Boot 3.x 对依赖版本要求较严格，升级依赖时需谨慎

## 📚 相关资源

- [MyBatis官方文档](https://mybatis.org/mybatis-3/)
- [MyBatis-Plus官方文档](https://baomidou.com/)
- [Spring Boot 3.x迁移指南](https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-3.0-Migration-Guide)

---

**修复时间**：2025-12-13  
**修复人员**：Kilo Code  
**状态**：✅ 已解决