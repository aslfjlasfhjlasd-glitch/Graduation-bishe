# 首次登录页面问题修复说明

## 🐛 发现的问题

### 问题 1：密码框自动填充
**现象**: 打开首次登录设置页面时，新密码和确认密码输入框被浏览器自动填充了之前保存的登录密码。

**原因**: 浏览器检测到密码输入框，根据之前的登录信息自动填入，或者把"新密码"误判为登录密码。

### 问题 2：标签不显示
**现象**: 兴趣标签和技能标签区域为空，没有显示任何标签选项。

**原因**: 前端代码使用的字段名（`type`, `name`, `id`）与后端返回的字段名（`bqLx`, `bqMc`, `bqId`）不一致。

## 🔧 修复方案

### 修复 1：禁用密码自动填充

#### 修改位置
文件：`frontend/src/views/student/FirstLoginSetup.vue`  
位置：模块二 - 密码修改区域

#### 修改内容
给两个密码输入框添加 `autocomplete="new-password"` 属性：

```vue
<!-- 新密码输入框 -->
<Input 
  v-model="passwordForm.newPassword" 
  type="password" 
  autocomplete="new-password"  <!-- 🔥 新增 -->
  placeholder="请输入新密码（至少6位，不能为123456）"
  class="bg-white"
  :class="{ 'border-red-500': passwordError }"
/>

<!-- 确认密码输入框 -->
<Input 
  v-model="passwordForm.confirmPassword" 
  type="password" 
  autocomplete="new-password"  <!-- 🔥 新增 -->
  placeholder="请再次输入新密码"
  class="bg-white"
  :class="{ 'border-red-500': passwordError }"
/>
```

#### 原理说明
`autocomplete="new-password"` 告诉浏览器：
- 这是用户正在创建的**新密码**
- 不要使用之前保存的登录密码自动填充
- 这是 HTML5 标准属性，所有现代浏览器都支持

---

### 修复 2：适配后端字段名

#### 后端数据结构
根据 `Tag.java` 实体类，后端返回的字段为：

```java
public class Tag {
    private Integer bqId;    // 标签ID
    private String bqMc;     // 标签名称
    private Integer bqLx;    // 标签类型：1-兴趣, 2-技能
    // ...
}
```

#### 修改 A：JavaScript 过滤逻辑

**位置**: `loadTags()` 函数

**修改前**:
```javascript
interestTags.value = tags.filter(tag => tag.type === '兴趣')
skillTags.value = tags.filter(tag => tag.type === '技能')
```

**修改后**:
```javascript
// 后端约定：bqLx=1 是兴趣/活动类别，bqLx=2 是技能
interestTags.value = tags.filter(tag => tag.bqLx === 1)
skillTags.value = tags.filter(tag => tag.bqLx === 2)
console.log('兴趣标签:', interestTags.value)
console.log('技能标签:', skillTags.value)
```

#### 修改 B：模板显示字段

**位置**: 模块三 - 兴趣与技能标签选择

**兴趣标签部分修改**:
```vue
<div v-if="interestTags.length > 0">
  <h4 class="text-sm font-medium text-gray-700 mb-2">兴趣标签</h4>
  <div class="flex flex-wrap gap-2">
    <label 
      v-for="tag in interestTags" 
      :key="tag.bqId"  <!-- 🔥 改为 bqId -->
      class="inline-flex items-center gap-2 px-3 py-2 rounded-full cursor-pointer transition-all"
      :class="selectedInterests.includes(tag.bqId)  <!-- 🔥 改为 bqId -->
        ? 'bg-blue-500 text-white shadow-md' 
        : 'bg-white text-gray-700 hover:bg-blue-100 border border-gray-300'"
    >
      <Checkbox 
        :checked="selectedInterests.includes(tag.bqId)"  <!-- 🔥 改为 bqId -->
        @update:checked="toggleInterest(tag.bqId)"  <!-- 🔥 改为 bqId -->
        class="hidden"
      />
      <span>{{ tag.bqMc }}</span>  <!-- 🔥 改为 bqMc -->
    </label>
  </div>
</div>
```

**技能标签部分修改**:
```vue
<div v-if="skillTags.length > 0">
  <h4 class="text-sm font-medium text-gray-700 mb-2">技能标签</h4>
  <div class="flex flex-wrap gap-2">
    <label 
      v-for="tag in skillTags" 
      :key="tag.bqId"  <!-- 🔥 改为 bqId -->
      class="inline-flex items-center gap-2 px-3 py-2 rounded-full cursor-pointer transition-all"
      :class="selectedSkills.includes(tag.bqId)  <!-- 🔥 改为 bqId -->
        ? 'bg-green-500 text-white shadow-md' 
        : 'bg-white text-gray-700 hover:bg-green-100 border border-gray-300'"
    >
      <Checkbox 
        :checked="selectedSkills.includes(tag.bqId)"  <!-- 🔥 改为 bqId -->
        @update:checked="toggleSkill(tag.bqId)"  <!-- 🔥 改为 bqId -->
        class="hidden"
      />
      <span>{{ tag.bqMc }}</span>  <!-- 🔥 改为 bqMc -->
    </label>
  </div>
</div>
```

## 📊 字段对照表

| 前端原字段 | 后端实际字段 | 说明 |
|-----------|-------------|------|
| `tag.id` | `tag.bqId` | 标签ID |
| `tag.name` | `tag.bqMc` | 标签名称 |
| `tag.type` | `tag.bqLx` | 标签类型（1=兴趣，2=技能） |

## 🧪 验证步骤

### 1. 验证密码自动填充修复
1. 清除浏览器缓存和已保存的密码
2. 重新登录并进入首次登录设置页面
3. 检查新密码和确认密码输入框是否为空
4. ✅ 预期：输入框应该是空的，不会自动填充

### 2. 验证标签显示修复
1. 确保数据库中有标签数据：
```sql
-- 查看标签数据
SELECT bq_id, bq_mc, bq_lx FROM t_bq WHERE zt = 1;

-- 如果没有数据，插入测试数据
INSERT INTO t_bq (bq_mc, bq_lx, zt) VALUES 
('篮球', 1, 1), ('足球', 1, 1), ('阅读', 1, 1),
('编程', 2, 1), ('设计', 2, 1), ('沟通', 2, 1);
```

2. 进入首次登录设置页面
3. 打开浏览器控制台，查看日志：
```
兴趣标签: [{bqId: 1, bqMc: '篮球', bqLx: 1}, ...]
技能标签: [{bqId: 4, bqMc: '编程', bqLx: 2}, ...]
```

4. 检查页面显示：
   - ✅ 兴趣标签区域显示蓝色标签
   - ✅ 技能标签区域显示绿色标签
   - ✅ 标签名称正确显示
   - ✅ 点击标签可以切换选中状态

## 🎯 完整测试流程

1. **清除数据**
   ```javascript
   localStorage.clear();
   ```

2. **登录**
   - 使用学号和密码 `123456` 登录
   - 应该跳转到首次登录设置页面

3. **检查密码框**
   - 新密码输入框应该为空
   - 确认密码输入框应该为空
   - 不应该有自动填充

4. **检查标签显示**
   - 兴趣标签区域显示多个蓝色标签
   - 技能标签区域显示多个绿色标签
   - 标签名称清晰可见

5. **测试标签选择**
   - 点击几个兴趣标签，应该变为蓝色高亮
   - 点击几个技能标签，应该变为绿色高亮
   - 再次点击已选中的标签，应该取消选中

6. **完成设置**
   - 输入新密码（如 `newpass123`）
   - 确认密码输入相同密码
   - 点击"完成设置"
   - 应该成功跳转到学生首页

## 🔍 调试技巧

### 如果标签仍然不显示

1. **检查后端接口**
```javascript
// 在浏览器控制台执行
fetch('http://localhost:8080/api/tags')
  .then(r => r.json())
  .then(data => console.log('标签数据:', data));
```

2. **检查数据库**
```sql
-- 确认有启用的标签
SELECT * FROM t_bq WHERE zt = 1;
```

3. **检查过滤逻辑**
```javascript
// 在 loadTags 函数中添加日志
console.log('原始标签数据:', tags);
console.log('过滤后的兴趣标签:', tags.filter(tag => tag.bqLx === 1));
console.log('过滤后的技能标签:', tags.filter(tag => tag.bqLx === 2));
```

### 如果密码仍然自动填充

1. **清除浏览器保存的密码**
   - Chrome: 设置 → 自动填充 → 密码 → 删除相关网站的密码
   - Firefox: 选项 → 隐私与安全 → 已保存的登录信息 → 删除

2. **使用无痕模式测试**
   - Chrome: Ctrl+Shift+N
   - Firefox: Ctrl+Shift+P

3. **检查 Input 组件**
   - 确认 `autocomplete="new-password"` 属性已正确传递到原生 `<input>` 元素

## 📝 修改文件清单

- ✅ `frontend/src/views/student/FirstLoginSetup.vue`
  - 第 68-87 行：添加 `autocomplete="new-password"`
  - 第 106-147 行：修改标签模板字段名
  - 第 273-279 行：修改标签过滤逻辑

## 🎉 修复完成

两个问题已全部修复：
1. ✅ 密码框不再自动填充
2. ✅ 标签正确显示并可选择

现在可以正常使用首次登录设置功能了！

---

**修复时间**: 2025-12-15  
**修复状态**: ✅ 已完成  
**测试状态**: 待验证